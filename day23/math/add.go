package math

// Add 2 numbers together.
func Add(leftAddend int, rightAddend int) int {
	return leftAddend + rightAddend
}

// Subtract performs basic subraction on two operands and return the remainder.
func Subtract(leftOperand, rightOperand int) int {
	return leftOperand - rightOperand
}
